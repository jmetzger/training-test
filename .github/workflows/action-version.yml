# .github/workflows/version.yml
name: Create PDF 
# 53w5

on: [workflow_dispatch]
#  push:
#   branches:
#     - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        
      - name: Show structure 
        run: |
          ls -la        
        
